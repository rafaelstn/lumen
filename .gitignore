# Python
__pycache__/
*.py[cod]
.pytest_cache/
.venv/
venv/

# Env / secrets
.env
*.env
!.env.example

# Node
node_modules/
dist/

# Jobs / dados sensíveis do cliente (uploads, logs, PDFs gerados)
backend/_jobs/
# Planilhas reais do cliente NUNCA entram no repo (dado fiscal confidencial).
# Fixtures de teste usam arquivos locais não versionados (ver tests/fixtures/).
*.xls
*.xlsx

# Docker
postgres_data/

# IDE / OS
.DS_Store
.idea/
.vscode/
